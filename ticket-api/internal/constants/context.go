package constants

const (
	// Claims -> authentication claims
	Claims = "Claims"

	// UID -> authenticated user's id
	UID = "UID"

	// StripeId  -> User's stripe id
	StripeId = "StripeId"

	// FirebaseId -> authenticated user's firebase id
	FirebaseId = "FirebaseId"

	// File uploaded file from file upload middleware
	File = "@uploaded_file"

	// Limit for get all api
	Limit = "Limit"

	// Page .
	Page = "Page"

	// RateLimit .
	RateLimit = "RateLimit"
)
